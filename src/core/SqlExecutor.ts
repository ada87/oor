// import type { QueryExecutor, ActionBuilder } from './types';

// export abstract class BaseExecutor<C, S> implements QueryExecutor<C, S> {

//     constructor() { }

//     abstract query(conn: C, sql: string, param?: Array<string | number | boolean | Date>): Promise<Array<S>>;
//     abstract get(conn: C, sql: string, param?: Array<string | number | boolean | Date>): Promise<S>;

    
// }