import { test } from 'tsest'
import _ from 'lodash'

import { initFromSQL } from './initDB';

test('initDB', () => {
    // initFromSQL('./dist/oor' + _.random(1000) + '.db', `CREATE TABLE data(key INTEGER PRIMARY KEY, value TEXT)`);
})
